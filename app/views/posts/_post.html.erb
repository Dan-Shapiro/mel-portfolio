<div id="<%= dom_id post %>">
  <p>
    <%= post.title %>
  </p>

  <p>
    <%= post.description %>
  </p>

  <% post.images.each do |image| %>
    <%= image_tag(image, style: "object-fit:contain; width:200px; height:200px;") %>
  <% end %>

</div>
